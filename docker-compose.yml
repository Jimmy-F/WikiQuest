version: '3.8'

services:
  # Backend API Server
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: wikihero-backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    environment:
      - NODE_ENV=development
      - PORT=3000
      - REDIS_URL=redis://redis:6379
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY:-}
    volumes:
      - ./backend:/app
      - /app/node_modules
    depends_on:
      - redis
    networks:
      - wikihero-network
    dns:
      - 8.8.8.8
      - 8.8.4.4
    command: npm run dev
    restart: unless-stopped

  # Background Worker (spaced repetition, analytics, cron jobs)
  worker:
    build:
      context: ./worker
      dockerfile: Dockerfile
    container_name: wikihero-worker
    environment:
      - NODE_ENV=development
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
      - REDIS_URL=redis://redis:6379
    volumes:
      - ./worker:/app
      - /app/node_modules
    depends_on:
      - redis
    networks:
      - wikihero-network
    dns:
      - 8.8.8.8
      - 8.8.4.4
    command: npm run dev
    restart: unless-stopped

  # Frontend Web Dashboard
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: wikihero-frontend
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
    volumes:
      - ./frontend:/app
      - /app/node_modules
    networks:
      - wikihero-network
    command: npm run dev -- --host 0.0.0.0
    restart: unless-stopped

  # Redis (caching + queue)
  redis:
    image: redis:7-alpine
    container_name: wikihero-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - wikihero-network
    command: redis-server --appendonly yes
    restart: unless-stopped

  # Marketing Website
  website:
    build:
      context: ./website
      dockerfile: Dockerfile
    container_name: wikihero-website
    ports:
      - "8081:80"
    networks:
      - wikihero-network
    restart: unless-stopped

networks:
  wikihero-network:
    driver: bridge

volumes:
  redis-data:
